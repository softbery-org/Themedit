<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Themedit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:src="clr-namespace:Themedit.src"
    x:Class="Themedit.PlaylistWindow"
    Title="Playlist"
    Width="300"
    Height="450"
    Closing="Window_Closing"
    MouseMove="Window_MouseMove"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <ListView
            x:Name="_listView"
            Margin="10,0,81,0"
            MouseDoubleClick="_playlist_MouseDoubleClick" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <ListView.View>
                <GridView>
                    <GridViewColumn />
                </GridView>
            </ListView.View>
        </ListView>
        <Button
            x:Name="BtnAddMedia"
            Margin="180,10,6,0"
            VerticalAlignment="Top"
            Click="BtnAddMedia_Click"
            Content="Add Media" HorizontalAlignment="Right" />
        <Button
            x:Name="BtnRemMedia"
            Margin="180,42,6,0"
            VerticalAlignment="Top"
            Content="Rem Media" HorizontalAlignment="Right" />
        <Button
            x:Name="BtnClrList"
            Margin="180,74,6,0"
            VerticalAlignment="Top"
            Click="BtnClrList_Click"
            Content="Clr Tracks" HorizontalAlignment="Right" />
        <Button
            x:Name="BtnClose"
            Margin="0,411,7,0"
            VerticalAlignment="Top"
            Click="BtnClose_Click"
            Content="Close" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Right" Width="67" >
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-1.5"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

    </Grid>
</Window>
